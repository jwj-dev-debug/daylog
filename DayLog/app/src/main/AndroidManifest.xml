<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DL_1">

        <!-- 🔒 앱 처음 실행 시 뜨는 잠금 화면 -->
        <activity
            android:name=".LockActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🔢 PIN 입력 화면 -->
        <activity android:name=".PinLockActivity" />

        <!-- 🔹 원래 메인 액티비티 (안 쓰면 그냥 놔둬도 됨) -->
        <activity android:name=".MainActivity" />

        <!-- 🔹 캘린더 화면 -->
        <activity android:name=".CalendarActivity" />

        <!-- 🔹 통계 화면 -->
        <activity android:name=".StatsActivity" />

        <activity android:name=".PinUnlockActivity" />

        <activity android:name=".PinVerifyActivity" />
        <activity android:name=".PinChangeActivity" />
        <activity android:name=".LockSettingsActivity" />
        <activity android:name=".PinResetActivity"/>

    </application>

</manifest>
